name: kNN/WKL_MAE_vs_kNN

sources:
  KSG:
    path: "./outputs/lowdim/continuous/WKL"
    aggregate: []
    pin: {}
    priority: 0

targets:
  - name: "MAE"
    function:
      _target_: __main__.MAE
      _partial_: true
      first: "target"
      second: "estimate.mean"

outputs: ["MAE"]

aggregation:
  by: "MAE"
  output: "MAE"

column_to_chart:
  name: "estimator.k_neighbors"
  apply:
    _target_: builtins.int
    _partial_: true

rows_to_chart: ["Distribution"]
pin: {}